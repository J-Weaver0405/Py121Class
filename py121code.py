# Function to calculate the Compound Interest
def comp_interest(p, r, t, n):
        r = r/100
        interest_amount = p * (pow(1+r/100 n * t)
        return interest_amount

p=float(input(f"Enter the principal{p} "))
r=float(input(f"Enter the rate of interest{r}"))
n=float(input(f"Enter the compounded time period{n}"))
t=float(input(f"Enter the number of years {t}”))

interest_val=comp_interest(p, r, t, n)
compound_interest=interest_val – p

print("The compounded amount: ", interest_val)
print("The compound interest: ", interest)


class finance:
    def __init__(self,):
        self.principal=float(input(“Enter the principal(p) ”))
        self.interest=float(input(“Enter the rate of interest(r) ”))
        self.compounded=float(input(“Enter the compounded time period(n) ”))
        self.time=float(input(“Enter the number of years(t) ”))




